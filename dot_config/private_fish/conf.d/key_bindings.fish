# Fish key bindings

# Emacs-style key bindings (equivalent to bindkey -e in zsh)
fish_default_key_bindings

# Custom key bindings for word movement
# Alt+Right Arrow - forward word
bind \e\e\[C forward-word
# Alt+Left Arrow - backward word  
bind \e\e\[D backward-word
